"# cloud-billing-dashboard" 
To run background : Go to parentfolder/backend/
    run this command in terminal : python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

To run Frontend 
 cd frontednd 
    npm install 
    npm run dev 